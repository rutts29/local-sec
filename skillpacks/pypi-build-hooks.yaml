name: pypi-build-hooks
version: 0.1.0
severity:
  setup_py: prompt
  pth_payload: prompt
  source_build: prompt
  credential_exfil: block
files:
  - setup.py
  - pyproject.toml
  - sitecustomize.py
  - "*.pth"

