name: macos-persistence
version: 0.1.0
severity:
  launch_agent: block
  launch_daemon: block
  osascript: prompt
paths:
  - Library/LaunchAgents
  - Library/LaunchDaemons
  - launchctl
  - osascript

